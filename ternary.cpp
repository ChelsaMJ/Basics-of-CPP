#include <iostream>  
using namespace std;  

int main() {  
    int n = -45;  
    cout << (n >= 0 ? "positive" : "negative") << endl;  
    //if-else turned into ternary statement
    return 0;  
}
